[package]
name = "backstory-mcp"
description = "backstory-mcp MCP server over stdio: six read-only session-forensics tools"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "backstory-mcp"
path = "src/main.rs"

# rmcp (and tokio with it) is added and pinned in T4.1 when the server is
# wired — pinning a pre-1.0 SDK weeks before use only invites churn (ADR A2).
[dependencies]
rmcp = { version = "2.2.0", features = ["server", "transport-io"] }
serde.workspace = true
serde_json.workspace = true
backstory-core = { path = "../backstory-core", version = "0.2.0" }
tokio = { version = "1.52.4", features = ["macros", "rt", "time"] }

[dev-dependencies]
backstory-core = { path = "../backstory-core", version = "0.2.0" }
tempfile = "3.27.0"
